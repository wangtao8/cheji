<!--pages/liPeiNews/index.wxml-->
<import src="../../utils/wxParse.wxml"/>
<view class='newInfo' wx:if='{{newVal.title}}'>
    <view>{{newVal.title}}</view>
    <view>时间：{{newVal.createtime}}</view>
</view>
<template is="wxParse" data="{{wxParseData:article.nodes}}"/>